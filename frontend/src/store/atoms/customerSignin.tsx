import {atom} from "recoil"

export const emailatom = atom({
    key: "emailatom",
    default : ""
})

export const customerpasswordatom = atom({
    key : "customerpasswordatom",
    default : ""
})